<?php
// замыкания
// замыкания в php применяются только к анонимным функциям

$mess = "текст до момента создания функции";

$test = function (array $some) use ($mess) {
	if (isset($some) && count() > )
} 
              
?> 